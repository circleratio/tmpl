# todo

plan.md に基づく実装・手動動作確認で判明した、対応方針が未決の事項。

- 未定義変数エラーなどでレンダリング途中に `TmplError` が送出された場合、それまでに作成済みのファイル・ディレクトリが出力先に残る（ロールバックされない）。spec.mdでは異常終了時の後始末について規定していないため、現状は未対応のまま。エラー時に出力先ディレクトリを削除するか等の方針は未検討。
- `copy_symlink` の `OSError` ラップ（Windowsで権限不足の場合に `TmplError` を送出する処理）は、この開発環境（開発者モード無効・管理者権限なし）ではテンプレート側にシンボリックリンクを作成すること自体ができず、エンドツーエンドでの動作確認はできていない。コード上は実装済み・ロジック上も妥当と判断するが、実機検証は持ち越し。

これまでここに記載していた項目（CLIオプションの追加要否、指示値のトリミング方針、シンボリックリンクの扱い、終了コードの粒度、対応OS、テスト方針、CI環境の要否とWindowsでのシンボリックリンクテストの扱い）は、ヒアリングの上 `requirement.md` / `spec.md` / `plan.md` に反映し解消済み。
