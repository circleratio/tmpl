# 概要
あるプロジェクトを開始する際に、初期のディレクトリ構成とファイル配置をテンプレートに基づいて行う。

実行形式は下記。
```
tmpl [種類(必須)] [プロジェクト名(必須)] [-o/--output 出力先(任意)] [--verbose(任意)] [--dry-run(任意)] [テンプレートへの指示1(任意)] .. [テンプレートへの指示N(任意)]
```

- 種類: テンプレートの種類。この指示にしたがって、テンプレートが選択される。テンプレートの配置場所は ~/share/tmpl/[種類] である。「~」はOSのホームディレクトリ（Windowsでは `%USERPROFILE%`、Linux/macOSでは `$HOME`）を指す。
- プロジェクト名: プロジェクトの名称。テンプレートの文字列置換で用いる（Jinja2変数名: `project_name`）。
- -o, --output: 生成したプロジェクトの出力先ディレクトリ。省略時はカレントディレクトリ直下に「プロジェクト名」のディレクトリを新規作成し、その中にテンプレートを展開する。
- --verbose: 展開したファイル・ディレクトリの一覧など、詳細なログを標準出力に表示する。
- --dry-run: 実際のファイル書き込みを行わず、展開されるファイル・ディレクトリの一覧のみを表示する。
- テンプレートへの指示1..N: テンプレートの文字列置換で用いる。形式は「変数名=値」である。この形式に従わない引数が渡された場合はエラーとし、異常終了する。値の前後の空白や引用符は加工せずそのまま用いる。

# 処理内容
1. 「種類」から対象のテンプレートディレクトリ（`~/share/tmpl/[種類]`）を特定する。存在しない場合はエラーメッセージを表示し、異常終了する。
2. テンプレートディレクトリ配下のファイル・ディレクトリを出力先にコピーする。その際、以下をJinja2でレンダリングし、変数を置換する。
   - ファイルの内容
   - ファイル名・ディレクトリ名（例: `{{ project_name }}.py` のような名前にも対応する）
   - 置換に使える変数は、`project_name`（プロジェクト名引数の値）と、「テンプレートへの指示」で指定された `変数名=値` の組である。
   - テンプレート内で未定義の変数が参照された場合はエラーとし、異常終了する（Jinja2の`StrictUndefined`を用いる）。
   - `.git` , `__pycache__` , `.DS_Store` などの一般的なファイル・ディレクトリはコピー対象外（除外）とする。除外パターンの詳細は別途定義する。
   - シンボリックリンクはリンク先を解決せず、リンクのまま複製する。
3. 出力先に同名のファイル・ディレクトリが既に存在する場合は、エラーメッセージを表示し、異常終了する（既存ファイルの上書きは行わない）。
4. `--dry-run` 指定時は、上記1〜3の判定・エラー検出は通常通り行うが、実際のディレクトリ作成・ファイル書き込みは行わない。

# 技術要件
- 使用する言語: Python
- テンプレート処理のエンジン: Jinja2
- 対応OS: Windows, Linux, macOS
